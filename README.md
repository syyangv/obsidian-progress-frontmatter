# obsidian-progress-frontmatter

An Obsidian plugin that renders a unicode progress bar into a note's frontmatter, opt-in per-note via `cssclasses`.

Planning in progress — see `~/.claude/plans/stateless-strolling-hamming.md`.
